function averageMark(algebra, geography, physics) {
    let sumMark = (algebra + geography + physics) / 3;
    return sumMark;
 }

function sayHello(userName) {
    let myName = userName;
    return `Привет, мир! Меня зовут  ${myName}`;
 }

function calculateFormula() {
    let x = 2;
    let y = 22;
    let z = 0;
    let solution = (x * y) + (5 * z) + (x -1);
    let result = solution
    return result;
 }
